# Plugin Flutter App SDK v1

A stable pure-C bridge exposes localized catalog JSON, installed packages, install/uninstall and processing to Dart FFI. The Digitor app owns commercial policy: paid plugins may be imported and previewed by free users, while unauthorized paid export is denied before engine processing. This layer is additive and does not modify the renderer, Primary Wheels, backend policy or plugin algorithms.
