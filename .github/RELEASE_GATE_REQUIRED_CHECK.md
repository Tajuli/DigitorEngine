# Required main-branch check

After the release qualification workflow is merged, configure branch protection for `main` to require the `Release gate` status check, require the branch to be up to date, and block direct pushes.
