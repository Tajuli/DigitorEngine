cmake_minimum_required(VERSION 3.20)
project(DigitorProductionTransformCrop LANGUAGES CXX)
add_executable(digitor_production_transform_crop main.cpp ../../src/gpu/production_transform_crop.cpp)
target_include_directories(digitor_production_transform_crop PRIVATE ../../include)
target_compile_features(digitor_production_transform_crop PRIVATE cxx_std_20)
if(MSVC)
  target_compile_options(digitor_production_transform_crop PRIVATE /W4 /WX)
else()
  target_compile_options(digitor_production_transform_crop PRIVATE -Wall -Wextra -Wpedantic -Werror)
endif()
