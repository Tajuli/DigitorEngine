cmake_minimum_required(VERSION 3.20)
project(digitor_production_video_denoise LANGUAGES CXX)
add_executable(digitor_production_video_denoise main.cpp ../../src/gpu/production_video_denoise.cpp)
target_include_directories(digitor_production_video_denoise PRIVATE ../../include)
target_compile_features(digitor_production_video_denoise PRIVATE cxx_std_20)
if(MSVC)
  target_compile_options(digitor_production_video_denoise PRIVATE /W4 /WX)
else()
  target_compile_options(digitor_production_video_denoise PRIVATE -Wall -Wextra -Wpedantic -Werror)
endif()
