# DigitorEngine v5.0.0 Phase 6 — HSL Qualifier Qualification

## Added qualification coverage

- Native HSL Qualifier target compiled on every host.
- CPU-reference versus native GPU numerical parity.
- CPU-source native execution and validation-only matte readback.
- GPU-source chaining with zero intermediate readback/re-upload.
- Deterministic repeated execution.
- Per-device pipeline-cache creation and reuse.
- Pipeline/program creation failure injection and recovery.
- Execution-provenance checks for native bindings and normal-preview readback prohibition.

## Hardware matrix

- Windows: D3D12 and Vulkan test entries.
- macOS: Metal test entry.
- Android: Vulkan and OpenGL ES test entries.
- Unsupported hosts return CTest skip code 77.

## Release truth

The source-level qualification harness is complete and Linux compile/regression validation is recorded by this phase. Actual production qualification remains dependent on successful execution on the corresponding Windows, macOS, and Android GPU runners. No hardware result is fabricated by this report.
